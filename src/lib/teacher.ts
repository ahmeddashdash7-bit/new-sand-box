/**
 * @license
 * SPDX-License-Identifier: Apache-2.0
 */

/**
 * Fixed teacher identity.
 *
 * The product is single-tenant (see CLAUDE.md), so the displayed teacher name is a constant
 * rather than a profile field. Import from here instead of hardcoding the string again.
 */
export const TEACHER_NAME = "Dr. Ghada Abdelaal";

/** Arabic form of the same name, used in parent-facing WhatsApp messages. */
export const TEACHER_NAME_AR = "د. غادة عبدالعال";
